# -*- coding: utf-8 -*-
"""Sanika Prac 5.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1rUMER4iMqqQJ9avkHoo5Z6tP1Xv0uN8U

Name: Sanika Takarkhede
USN No: CS23016

Aim: To implement the Random Forest algorithm and evaluate the model performance using appropriate metrics import accuracy_score, classification_report
"""

import pandas as pd
from sklearn.model_selection import train_test_split
from sklearn.ensemble import RandomForestClassifier
from sklearn.metrics import accuracy_score,classification_report

from google.colab import drive
drive.mount('/content/drive')

path="/content/drive/MyDrive/ML_dataset/banking"
df=pd.read_csv(path)
df.head(5)

df.shape

df.columns

df.isnull().any().any()

df[df.isnull().any(axis=1)]

df.info()

x = df.drop(columns=['y'])
y = df['y']

x.head()

print(x)

x = pd.get_dummies(x,dtype=int)

x.head()

x.shape

x.columns

print(x)

from sklearn.model_selection import train_test_split
x_train,x_test,y_train,y_test=train_test_split(x,y,train_size=0.7,test_size=0.3,random_state=42)
x_train.shape,x_test.shape

from sklearn.ensemble import RandomForestClassifier
rf_classifier=RandomForestClassifier(n_estimators=100,n_jobs=-1, max_depth=5, oob_score=True, random_state=42)
rf_classifier.fit(x_train,y_train)

#prediction
y_pred=rf_classifier.predict(x_test)
#

from sklearn.metrics import confusion_matrix,accuracy_score,classification_report

print('Confusion Matrix')
cm = confusion_matrix(y_pred,y_test)
print(confusion_matrix(y_pred,y_test))

print('Accuracy Score')
print(accuracy_score(y_pred,y_test))

print('Classification Report')
print(classification_report(y_pred,y_test))

rf = RandomForestClassifier(random_state=42, n_jobs=-1)

params = {
    'max_depth': [2,3,5,10,20],
    'min_samples_leaf': [5,10,20,50,100,200],
    'n_estimators': [10,25,30,50,100,200]
}

from sklearn.model_selection import GridSearchCV
grid_search = GridSearchCV(estimator=rf, param_grid=params, cv=4, n_jobs=-1, verbose=1, scoring="accuracy")

grid_search.fit(x_train,y_train)

grid_search.best_score_

print(grid_search.best_params_)