# -*- coding: utf-8 -*-
"""prac 6 Extra  MLSanika (CS23016)

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1Zi__N0t9dzPeTdzUHWqOZFB3nwSH6V6_

Sanika (CS23016)
Aim: To design and implement an Artificial Neural Network for classification problem and evaluate its learning performance.
"""

import pandas as pd
from sklearn.model_selection import train_test_split

from google.colab import drive
drive.mount('/content/drive')

path= "/content/drive/MyDrive/ML_DATASET/Heart_Failure_Prediction_dataset - Heart_Failure_Prediction_dataset.csv"
df=pd.read_csv(path)
df.head(5)

df.shape

df.columns

df.describe()

df.info()

df.isnull().any().any()

df[df.isnull().any(axis = 1)]

df.isnull().sum()

X=df.drop(columns=['HeartDisease'])
y=df['HeartDisease']

X.head()

#X=pd.get_dummies(X)

X

X.shape

X.columns

X=pd.get_dummies(X,dtype=int)

X

from sklearn.model_selection import train_test_split
X_train,X_test,y_train,y_test=train_test_split(X,y,train_size=0.7,test_size=0.3,random_state=42)
X_train.shape,X_test.shape

X=df.iloc[:,:3:-1]
X

y=df.iloc[:,-1]
print(y)

X=pd.get_dummies(X,dtype=int)

X

from sklearn.preprocessing import StandardScaler
scaler=StandardScaler()
X_train=scaler.fit_transform(X_train)
X_test=scaler.transform(X_test)

X_train

import numpy as np
import pandas as pd
import tensorflow as tf

ann=tf.keras.models.Sequential()

ann.add(tf.keras.layers.Dense(units=6,activation='relu'))

ann.add(tf.keras.layers.Dense(units=6,activation='relu'))

ann.add(tf.keras.layers.Dense(units=1,activation='sigmoid'))

ann.compile(optimizer='adam',loss='binary_crossentropy',metrics=['accuracy'])

ann.fit(X_train,y_train,batch_size=32,epochs=100)

y_pred=ann.predict(X_test)
y_pred=(y_pred>0.5)
y_pred

from sklearn.metrics import confusion_matrix,accuracy_score,classification_report

print('Confusion Matrix')
cm=confusion_matrix(y_pred,y_test)
print(confusion_matrix(y_pred,y_test))

print('Accuracy Score')
print(accuracy_score(y_pred,y_test))

print('classification Report')
print(classification_report(y_pred,y_test))