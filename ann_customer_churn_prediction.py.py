# -*- coding: utf-8 -*-
"""Sanika prac 6 ML.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1PTS8ZIqbDwpwYzkzLhlRxRVvNh5kR1uG

To design and implement an Artificial Neural Network for classification
problem and evaluate its learning performance.
"""

from google.colab import drive
drive.mount('/content/drive')

#Sanika Takarkhede(CS23016)

import pandas as pd
from sklearn.model_selection import train_test_split
from sklearn.ensemble import RandomForestClassifier
from sklearn.metrics import accuracy_score,classification_report

path ="/content/drive/MyDrive/ML_dataset/churn - churn.csv"
df=pd.read_csv(path)
df.head(5)

df.shape

df.columns

df.isnull().any().any()

X = df.drop(columns=['Exited'])
y = df['Exited']

X.head()

X = pd.get_dummies(X,dtype = int)

X.head()

X = df.iloc[:,3:-1]
X = pd.get_dummies(X, dtype=int)
X

y=df.iloc[:,-1]
print(y)

X.shape

X.columns

from sklearn.model_selection import train_test_split
X_train,X_test,y_train,y_test = train_test_split(X,y,train_size=0.7,test_size=0.3,random_state=42)
X_train.shape,X_test.shape

from sklearn.preprocessing import StandardScaler
sc = StandardScaler()
X_train = sc.fit_transform(X_train)
X_test = sc.transform(X_test)

X_train

y_train

import tensorflow as tf

#initialising ANN
ann = tf.keras.models.Sequential()

#adding first hidden layer
ann.add(tf.keras.layers.Dense(units=6,activation="relu"))

#adding second hidden layer
ann.add(tf.keras.layers.Dense(units=6,activation="relu"))

#Adding output layer
ann.add(tf.keras.layers.Dense(units=1,activation="sigmoid"))

#compiling the ann
ann.compile(optimizer="adam",loss="binary_crossentropy",metrics=['accuracy'])

#fitting  the ann to the training set
ann.fit(X_train,y_train,batch_size=32,epochs=100)

y_pred = ann.predict(X_test)
y_pred = (y_pred > 0.5)
y_pred

from sklearn.metrics import confusion_matrix,accuracy_score

print('Confusion Matrix')
cm=confusion_matrix(y_pred,y_test)
print(confusion_matrix(y_pred,y_test))

print('Accuracy Score')
print(accuracy_score(y_pred,y_test))